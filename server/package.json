{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for IEST Inventarios",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd ../client && npm install && npm run build",
    "render-build": "npm install && npm run build"
  },
  "dependencies": {
    "express": "^5.1.0",
    "cors": "^2.8.5",
    "bcrypt": "^5.1.1",
    "pg": "^8.15.5",
    "dotenv": "^16.5.0",
    "express-session": "^1.18.1",
    "connect-pg-simple": "^10.0.0",
    "multer": "^1.4.5-lts.2",
    "body-parser": "^1.20.2",
    "pg-pool": "^3.9.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}